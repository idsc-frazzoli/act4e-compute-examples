mcdp {
  provides velocity [m/s]
  requires batt_cost [USD]

  sub battery_dp = instance `battery_choices
  sub chassis_dp = instance `chassis_and_motor

  provided velocity ≤ velocity provided by chassis_dp
  batt_cost required by battery_dp ≤ required batt_cost
  
  power required by chassis_dp ≤ power provided by battery_dp
  batt_mass required by battery_dp ≤ total_load provided by chassis_dp 
  
}
